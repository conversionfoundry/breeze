<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title><%= [ yield(:title), "Breeze" ].compact.join(" | ") %></title>
    <%= stylesheet_link_tag "/breeze/stylesheets/admin" %>
    <%= javascript_include_tag "http://ajax.googleapis.com/ajax/libs/jquery/1.4.2/jquery.min.js", "/breeze/javascripts/rails", "/breeze/javascripts/admin" %>
  </head>

  <body>
    <div id="header">
      <div class="top">
        <a class="logo" href="/admin">Breeze</a>
        <% if signed_in? -%>
          <div id="signed-in-user">
            Signed in as <%= link_to current_user, edit_admin_user_path(current_user), :class => :user %> |
            <%= link_to "Sign out", destroy_admin_session_path %>
          </div>
        <% end -%>
      </div>
    </div>
    <%= flash_messages %>
    <% main, left, right = [ yield, yield(:left), yield(:right) ].map(&:strip) -%>
    <% if left.blank?; left = pane_layout {}; end -%>
    <% if right.blank? -%>
      <%= content_tag :div, main, :id => :main, :class => "no-sidebar" %>
      <%= content_tag :div, left, :id => :left %>
    <% else -%>
      <%= content_tag :div, main, :id => :main %>
      <%= content_tag :div, left, :id => :left %>
      <%= content_tag :div, right, :id => :right %>
    <% end -%>
  </body>
</html>