$('#left #themes a[href="/admin/themes/<%= theme.name %>/files"]').parent().each(function() {
  $(this).<%= theme.enabled? ? :add : :remove %>Class('enabled');
  advance_slider($(this).closest('.sliding'), -1);
});