<% if @page.errors.empty? %>
  $('#page_new').closest('.ui-dialog-content').dialog('close');
  $('#page_new').closest('.ui-dialog').remove();
  $('#page_<%= @page.parent_id %>').replaceWith('<%= escape_javascript render(:partial => "page", :object => @page.parent) %>');
  $('#page_<%= @page.id%>>a').click();
<% else %>
  $('#page_new').replaceWith('<%= escape_javascript render(:template => "/breeze/admin/pages/new") %>');
<% end %>
