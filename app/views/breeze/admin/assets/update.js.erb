$('#asset_<%= @asset.id %>').replaceWith('<%= escape_javascript render(:partial => (@asset.image? ? "breeze/admin/assets/image" : "asset"), :object => @asset) %>');
$('#edit_asset').closest('.ui-dialog-content').dialog('close').remove();