$('#asset_<%= @asset.id %>').replaceWith('<%= escape_javascript render(:partial => (@asset.image? ? "image" : "asset"), :object => @asset) %>');
$('#asset_edit').closest('.ui-dialog-content').dialog('close').remove();