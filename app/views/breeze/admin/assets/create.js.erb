<% if @asset.errors.empty? %>
  $('#assets .<%= @asset.class.name.demodulize.underscore %>-assets').prepend('<%= escape_javascript render(:partial => @asset.class.name.demodulize.underscore, :object => @asset) %>');
  show_or_hide_asset_section_headings();
<% end %>